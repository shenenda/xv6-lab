当两个 put 线程同时向同一个散列桶插入不同的键时，它们可能都先读取到相同的旧桶头 H。
线程 1 构造节点 A 并令 A->next = H；线程 2 构造节点 B 并令 B->next = H。
如果线程 2 先执行 table[i] = B，随后线程 1 又执行 table[i] = A，那么 B 将从链表中丢失。
因此，查找、判断和插入必须作为同一个受桶锁保护的临界区；不同桶使用不同锁，仍允许无冲突的 put 并行执行。
